# 未来独立最终测试集准入规则

当前状态：`EMPTY_LOCKED_INTAKE`，尚未放入任何音频。

准入前必须满足：

1. 数据来源此前未参与训练、验证、阈值选择和结构设计。
2. 以录音、会话、设备、地点和型号为组进行隔离，禁止窗口级随机拆分。
3. 与所有历史数据执行文件哈希、解码 PCM 哈希和必要的近重复检查。
4. 在解锁标签和结果前冻结代码版本、模型权重、推理流程与阈值。
5. 最终集只执行一次正式评估；后续据此改进的模型必须使用另一份新测试集。

数据准入位置为 `data/future_final_holdout/`。录入前先更新 `DATASET_REGISTRY.csv` 并生成独立审计报告。

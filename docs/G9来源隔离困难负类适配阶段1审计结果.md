# G9：来源隔离困难负类适配阶段1审计结果

审计日期：2026-07-20  
当前状态：数据准备门通过；GPU preflight 尚未运行；正式训练尚未启动。

## 1. 阶段1结论

`G9-HN-BCE` 的来源隔离困难负类清单与缓存已在当前 ABDDV 工程中实际生成。
准备过程只读取 DADS、预注册的 `val_ood` tune/holdout 身份数据和 U16/ESC-50
候选池，没有读取 Unseen/Real-world；`locked_datasets_read: []`。

所有已注册隔离项均为零：

- 候选与 DADS `source_path` 交叉：0；
- 候选与 DADS 原始音频 SHA256 交叉：0；
- 候选与 `val_ood` 路径、source group、生成样本 SHA256、原始来源 SHA256
  交叉：0；
- HN train/guard 的 recording group、Freesound source group、原始 clip ID 和
  SHA256 交叉：0。

因此数据门通过，可以进入真实 GPU preflight；这不等于模型效果已经通过，
也不授权跳过 preflight 直接训练。

## 2. 实际数据规模

| 项目 | 数值 |
|---|---:|
| 六类原始目标片段 | 1,200 |
| `RMS > -65 dBFS` 有效片段 | 1,186 |
| HN train 片段 | 946 |
| HN guard 片段 | 240 |
| HN train ESC recording groups | 192 |
| HN guard ESC recording groups | 48 |
| HN train Freesound source groups | 121 |
| HN guard Freesound source groups | 38 |
| 原 DADS manifest 行 | 305,006 |
| 合并 manifest 行 | 305,952 |
| 合并后 train 行 | 213,448 |
| 合并后 train 正类 | 124,528 |
| 合并后 train 负类 | 88,920 |
| 自动 `pos_weight=negative/positive` | 0.7140562765 |

每个类别仍保留32个官方 fold 1–4训练 recording groups 和8个 fold 5 guard
recording groups。14个近静音片段被固定 RMS 规则排除，但没有任何 recording
group 被全部删除。

## 3. 不可变产物

| 产物 | SHA256 |
|---|---|
| `audit.json` | `a56e353b320fe48e143c54eaf60919db5f893b0a0e35d7d20d1b865e7e29807d` |
| `hn_train.csv` | `d2d9c39c968a30d0043203cc13b9e619f98cb51676f6de07b83c48cb8471c109` |
| `hn_guard.csv` | `7e834817ffca2b033d9024a53ebef87e8b29efd79ddf24a70c8c0c406e44bb6c` |
| `dads_g9_hn_seed42.csv` | `b8df5c7e5de7e657e7d24d4f20431282d8687faebb3fcdde37c12d35257a8898` |
| 1,186个 cache 的树哈希 | `24e35d79119f78d15c34b3bd193fc877627eb5ff68db9d1592b63249847e491a` |

独立复核确认：原 DADS 的305,006行在全部原字段上逐行保持一致；新增的946行
全部为 `split=train`、`label=0`、`segment_kind=hard_negative`，且
`background_mix_eligible=false`。重复执行 prepare 会拒绝覆盖现有产物。

## 4. 训练机制复核

G9 相对 G7-PT 的训练配置只增加数据审计绑定并更换输出目录。模型、AudioSet
初始化、batch、epoch、学习率、增强、AMP、普通 BCE、自然无放回 shuffle 和
seed 42 全部保持不变。困难负类可以作为直接负类训练，但不会进入给正类混入
背景的增强采样池。

训练的 `best.pt`、`last.pt` 和 `metrics.json` 会绑定合并 manifest 与
`audit.json` 的路径、行数及 SHA256；任何变化或不匹配的断点都会失败关闭。
非交互日志已取消动态回车进度条，改为每100 step输出一条可直接 `tail -f`
查看的记录。

代码编译、shell语法和完整单元测试均通过：`Ran 66 tests, OK`。

## 5. 下一门：GPU preflight

在服务器终端运行：

```bash
cd /home/user1/JJZ/ABDDV-CRNN
bash scripts/run_g9_hn_stage1.sh preflight \
  2>&1 | tee logs/g9_hn_stage1_preflight.log
```

只有输出同时满足以下条件才可启动训练：

- `"passed": true`；
- `train_logits_finite`、`val_logits_finite`、`train_gradients_finite` 均为 true；
- `natural_sampling_without_replacement: true`；
- `hard_negative_samples_in_batch >= 1`；
- `locked_datasets_read: []`；
- 参数量仍为79,955,457，官方预训练 checkpoint SHA256 不变。

许可边界仍然有效：ESC-50 为 CC BY-NC，本地非商业研究可以继续；在恢复逐
clip attribution/LICENSE 前，数据、缓存和训练模型均不得对外发布，当前
`publication_ready=false`。
